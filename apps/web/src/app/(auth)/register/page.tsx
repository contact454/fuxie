import { RegisterClientDynamic } from '@/components/auth/RegisterClientDynamic'

export const metadata = {
    title: 'Fuxie - Register',
    description: 'Create a Fuxie account',
}

export default function RegisterPage() {
    return <RegisterClientDynamic />
}
