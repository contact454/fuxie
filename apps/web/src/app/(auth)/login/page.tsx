import { LoginClientDynamic } from '@/components/auth/LoginClientDynamic'

export const metadata = {
    title: 'Fuxie - Login',
    description: 'Sign in to Fuxie',
}

export default function LoginPage() {
    return <LoginClientDynamic />
}
