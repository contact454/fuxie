# Fuxie Takeover Baseline — 2026-09-11

Timezone: Asia/Ho_Chi_Minh

This is a dated snapshot, not a live status source. Re-read GitHub/deployment/provider metadata before making current-state claims.

## Repository state observed

- Repository: `contact454/fuxie`.
- Default branch: `master`.
- Observed master SHA: `c83760fadf57dae9319ebe8f4c64233602230ff0`.
- Observed branch protection: `master` returned `protected: false`; required checks were not enforced in the branch metadata read during takeover.
- Active large PR: #21 `chore/content-audit-remediation-2026-06`.
- PR #21 observed head: `bdf7fd10780bd57397b8ac3944e8c9c35a68ed01`.
- PR #21 observed state: open, not merged; 78 commits; 1,408 changed files in the metadata read on 2026-09-11.
- Management bootstrap branch created successfully: `chore/management-control-plane-2026-09-11`, based on master.

## CI state observed for PR #21

GitHub Actions run `34578063503` was observed as failed. The `Quick checks` job failed at dependency installation; downstream `Full verify + AI eval gate` and `Production Hardening Smoke Test` were skipped.

Allowed conclusion: the observed PR head does not have a complete green release-gate run from this workflow execution.

Not allowed: inferring the code itself is necessarily broken without reading the install error, or reusing a prior green run for this head.

## Session-completion code recheck

At PR #21 head `bdf7fd1...`, the previously observed missing SRS-card ownership filter has a code change: the route uses an update constrained by both `cardId` and `userId`, and checks the affected row count.

Allowed conclusion: the ownership defect has a code-level remediation in the PR tree read during takeover.

Not yet proved by this statement: regression test coverage, merge to master, deployment to production, or absence of other authorization paths.

The same route still accepted client-provided `totalXp` and recorded lesson completion in the code read during takeover. This requires separate triage/testing before being considered resolved.

## Content quality state reported by project docs

The observed `docs/content-quality/audit-2026-06/STATUS.md` at PR #21 head reported:

- machine QA passing across tracked content cells;
- academic sign-off only 1/36 cells signed, 35/36 pending;
- 6/6 listening cells with audio work still pending;
- remaining manual/native review work for deeper language/level/semantic/audio/blueprint dimensions.

These are reported project states, not an independent re-run or human sign-off by the orchestrator.

## Deployment platform observed

Vercel access was verified for project:

- name: `fuxie-web`;
- project id: `prj_jxTRrBqdN2qZBc2wqMgVrh1DQgzD`;
- configured domains included `fuxie.edu.vn` and Vercel domains;
- latest deployment observed: `dpl_BV5Ku2dQYMtP115YvnWcG21g8heM`;
- latest deployment state observed: `ERROR`;
- latest deployment target observed: `null`;
- project metadata returned `live: false` during this read.

No deployment, promotion, rollback or domain change was executed during bootstrap. A fresh read is required before any statement about the current production state.

## Database/provider state observed

Neon project identity was not verified. The connector `list_projects` path required an organization id that was not yet known; a search returned no Fuxie resource. No database write, migration or query was attempted.

Firebase, Redis/Upstash and AI/STT/TTS provider identities/production environments were not independently verified during this bootstrap snapshot.

## Governance mismatch found

The repository operating docs hard-coded a workflow in which Claude planned/managed/QCed, Antigravity implemented code and Codex rendered assets. The owner has now assigned the project-management function to the current Fuxie project orchestrator.

Bootstrap action: generalize governance around capabilities (`Orchestrator`, `Executor`, `Reviewer/QA`, `Asset renderer`) while preserving the mandatory domain-role gate. Tool/product names become mappings only after their access is verified.

## Current bootstrap work

- BOOT-A — unify management authority and agent delivery semantics: IN PROGRESS on management branch.
- BOOT-B — establish source registry and evidence semantics: IN PROGRESS.
- BOOT-C — verify tool/service map: PARTIAL (GitHub write + Vercel read verified; Neon blocked; others pending).
- BOOT-D — run management-system smoke cases: NOT RUN.
- BOOT-E — re-baseline technical/security/content gates on a selected current tree: NOT RUN.
- BOOT-F — drive one bounded implementation slice end-to-end: NOT RUN.

## Rules for using this snapshot

- Never treat this file as live deployment or CI state.
- Prefer fresh connector/API reads for current state.
- Preserve exact SHA/environment when citing evidence.
- If PR #21 changes, any code/content conclusion that depends on changed files must be re-read.
- If deployment/provider configuration changes, re-run the relevant operational checks.