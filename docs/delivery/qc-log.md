# Delivery QC Log

Running QC record for the three-agent delivery model
(`.agents/workflows/three-agent-delivery-model.md`). Claude is QC; entries are
plan reviews and code reviews against the `docs/delivery/` spec acceptance criteria.

---

## 2026-06-03 — Plan review: Slice A + Slice B (Antigravity)

- **Artifact:** Antigravity `implementation_plan.md` for `slice-A-session-p0.md` + `slice-B-dashboard-wowgap.md`.
- **Verdict:** ✅ **Approved to implement, with conditions.** Plan is faithful to both specs and references the correct files (reuse `dashboard-client.tsx:581-609`, `quest-visuals.tsx`, audio idiom `lesson-player.tsx`, `builder.ts`, `types.ts`). No new assets — reuse-first respected.
- **Coverage:** Slice A R-1…R-7 covered; Slice B R-1…R-4 covered.

### Accepted scope change
- **R-8 (distractors) done at source, not deferred.** Antigravity will fix `lib/session/builder.ts` to draw distractors from same-theme vocabulary instead of the hardcoded client list. Approved (correct root fix), subject to the guardrails below.

### Conditions — builder.ts guardrails (must honor)
1. Fallback when a theme has <3 other words → still produce 4 valid options.
2. One batched query per theme (no N+1 across session items).
3. Apply only to VOCAB items; review cards without `themeId` need a safe branch.
4. `correctIndex` must match the post-shuffle position; add a small builder unit test if none exists.
5. Confirm the client-side fallback (`MultipleChoice.tsx:38-78`) is now unreachable → keep as safety net or remove, and state which.

### Confirm-points during implementation (non-blocking)
1. Slice B: split the status flag at `DashboardMockupClient.tsx:425` (`claimed` → ✓, `claimable` → Claim button; don't conflate).
2. Slice B: mission list must read from **local** `missionBoard` state (re-render after claim).
3. Slice B: do NOT edit `page.tsx` or `DashboardClientDynamic.tsx`; add no new fetch.
4. Slice A: timer must stop when `isFinished` and clear interval on unmount.
5. Slice A: do not touch `handleNext`/hearts/score/results.

### Code QC on return (pending)
Run Slice A's 8 acceptance items + Slice B's 7 items; review `git diff` for scope-creep;
confirm `pnpm check:quick`, `pnpm test:core`, `pnpm build` green; manual: 1 audio item /
1 no-audio item / 1 A2+ session / 1 mission claim.
- **Status:** ✅ resolved — see code review below.

---

## 2026-06-03 — Code review: Slice A + Slice B (Antigravity)

- **Verdict:** ✅ **ACCEPTED** (both slices). Diffs reviewed line-by-line; requirements + confirm-points met. Two builder fast-follows required (below). One pre-existing gate failure surfaced (not caused by this work).
- **Files changed (expected only):** `session/exercises/MultipleChoice.tsx`, `session/SessionPlayer.tsx`, `lib/session/builder.ts`, `dashboard/DashboardMockupClient.tsx`. `public/sw.js` regenerated by build (expected artifact). `page.tsx` + `DashboardClientDynamic.tsx` confirmed untouched. No new assets.

### Slice A — PASS
R-1 real audio (ref + autoplay + play/pause, browser-block tolerant) ✅ · R-2 `mockAudio` removed ✅ · R-3 waveform gated on `isPlaying`, dynamic badge HÖREN/WORTSCHATZ/GRAMMATIK ✅ · R-4 misleading tip removed → post-check grammar explanation ✅ · R-5 `{level}` in header + DEIN WEG ✅ · R-6 real timer, stops on `isFinished`/`loading`, clears on unmount, reset button removed ✅ · R-7 nav rail → `aria-hidden` scenery ✅ · hearts/score/results untouched ✅.

### Slice B — PASS
R-1 Fucoin wallet chip (desktop+mobile) from live `missionBoard.wallet.balance` ✅ · R-2 claimable rows → Claim button, POST `/api/v1/missions/{id}/claim`, swaps `missionBoard` from response ✅ · R-3 `RewardRevealMoment` on success ✅ · R-4 spinner + double-submit guard + learner-facing error ✅ · R-5 status flag split (`isClaimed`/`isClaimable`), list reads local `missionBoard`, no plumbing changes ✅.

### Required fast-follows (builder.ts — Slice A-fix)
1. **Empty correct answer guard.** `correctWord = (translations as any)?.vi || ''` → if `vi` missing, the correct MC option is BLANK (builder now always supplies `options`, so the client safety-net no longer rescues this). Fix: fall back to German `term` when `vi` empty; if still empty, downgrade item to `TYPING`.
2. **Remove N+1.** Distractor `findMany` runs inside the per-card loop. Batch same-theme + same-CEFR candidates in one/two queries before the loop; build options from the in-memory pool. Keep the default-fallback.

(Minor nits, non-blocking: claimable row shows a green ✓ icon alongside the Claim button; mobile Fucoin coin uses `animate-pulse`; leftover `console.log` in MultipleChoice autoplay/click. Address opportunistically.)

### Pre-existing gate finding (NOT a Slice A/B regression)
`pnpm check:quick` fails at `check:asset-audit`: optimized-asset coverage **49% (50/102) < 95% threshold**. But the **orphans check is clean** — all 52 unreferenced files are intentionally listed in `docs/design/asset-archive.md`. So the coverage threshold is mis-calibrated (it doesn't credit archived files); this is red independent of this work and cannot be caused by a diff that *adds* a reference. **Action:** recalibrate the gate (coverage = referenced / (total − archived), or adjust threshold) as a separate DevOps work order — do not block feature slices on it. NOTE: `check:quick` here runs asset/locale/visual/property audits, not `tsc`; typecheck is covered by the passing `pnpm build`.

### Antigravity transparency note
The handoff report listed test:core + build as green but omitted that `check:quick` fails. Per the report contract, **report ALL gates including failures** (even pre-existing) so QC sees the full picture.

### Builder fast-follow prompt (copy-paste)
```
ROLE: Engineer for Fuxie, fixing two QC findings in the Slice A builder change. Fixed scope; do not expand.
OBJECTIVE: Harden apps/web/src/lib/session/builder.ts MULTIPLE_CHOICE distractor generation (VOCAB_REVIEW).
FIX 1 (empty correct answer): correctWord = (translations)?.vi || ''. If vi is missing the correct option is blank. Fall back to the German term (card.vocabularyItem.term) when vi is empty; if still empty, set this item's format to TYPING instead of MULTIPLE_CHOICE (do not emit a blank option).
FIX 2 (N+1): the distractor findMany runs inside the per-card loop. Before the loop, gather the themeIds/level of all MC review cards and fetch candidate same-theme + same-CEFR words in one/two batched queries; build each item's options from the in-memory pool. Keep the default hardcoded fallback as last resort. Preserve correctIndex correctness after shuffle.
ACCEPTANCE: every MC review item has exactly 4 non-empty options with correctIndex on the correct (non-empty) answer; zero per-card DB queries in the loop; pnpm test:core + pnpm build green; report check:quick result honestly (asset-audit coverage is a known pre-existing failure — note it, don't try to fix it here). EDIT ONLY builder.ts.
REPORT: diff hunks; confirm no per-item query remains; gate outputs (all of them).
```
- **Status:** A & B accepted into branch; builder fast-follow dispatched; Slice C-1 cleared to hand off.

---

## 2026-06-03 — Code review: Slice A-fix (builder) + Slice C-1 (shell parity)

- **Verdict:** ✅ **ACCEPTED** (both). Diffs reviewed; build + state-shell-coverage green. No interiors touched; `quest-visuals.tsx` correctly skipped (optional/last). No new assets.

### Slice A-fix (builder.ts) — PASS
- FIX-1 empty correct answer: `vi` missing → falls back to German `word`; if still empty → format downgraded to `TYPING` (never a blank option). ✅
- FIX-2 N+1 removed: theme candidates (`themeId in [...]`) + CEFR candidates (`take 50`) fetched in **two batched queries before the loop**; per-card logic filters in-memory. ✅
- `correctIndex` correct after splice; always exactly 4 non-empty options. ✅
- Nits (non-blocking): German-word-as-meaning on the rare missing-`vi` case (pedagogically odd but not broken); theoretical dup if correctWord literally equals a hardcoded fallback.

### Slice C-1 (shell parity) — PASS
- Files: `globals.css`, `mobile-shell.tsx`, `sidebar.tsx`, `skill-motivation-layer.tsx` only. Deep-blue chrome `#064987` + borders `#8bd3ff/30` + active teal `#2EC4B6` + amber `#FFB703` accents, applied to mobile header, bottom-nav, desktop sidebar, and the shared motivation banner. ✅
- D2 respected (no desktop top bar added). D3 respected (no Fucoin/level chip; XP chip kept, restyled amber). ✅
- No module interior / `page.tsx` / `layout.tsx` touched. `isMockupAlignedRoute` (dashboard/session) bypass intact → unaffected. ✅

### Gate status (independently run by QC)
- `pnpm build`: ✅ PASS (typecheck + compile).
- `check:state-shell-coverage`: ✅ PASS.
- `check:quick`: ❌ bails at `check:asset-audit` (pre-existing coverage-threshold issue; orphans clean).
- `check:locale-parity`: ❌ 364 violations — **pre-existing repo-wide debt** (hardcoded DE/VN copy in mock-styled surfaces). Our work added exactly ONE new string (`MultipleChoice.tsx:299 "💡 Erklärung:"`) and removed the old hardcoded tip → net neutral. C-1 added zero.
- `check:visual-audit`: ❌ 166 violations — **pre-existing** (missing screenshot captures in `qa-runs/2026-05-16`; run `pnpm test:integration:capture`). Not caused by CSS diffs.

### Honest "definition of done" note
The repo's `check:quick`, `check:locale-parity`, and `check:visual-audit` gates are **red at baseline**, independent of A/B/A-fix/C-1. Feature slices pass the gates they can affect (build, state-shell). The pre-existing gate debt must be cleared as separate cleanup work orders before any "all gates green" release claim.

### Tiny cleanup (next fast-follow, non-blocking)
Wrap `MultipleChoice.tsx:299 "💡 Erklärung:"` in `t()` (or `// locale-allow`) so we don't add to locale debt.

### Suggested separate cleanup work orders (DevOps/i18n, not feature slices)
1. Recalibrate `check:asset-audit` coverage to credit archived files (`asset-archive.md`).
2. i18n backlog: move hardcoded DE/VN copy in `DashboardMockupClient.tsx` / `SessionPlayer.tsx` (and others) into `messages/{vi,de}.json`.
3. Run `pnpm test:integration:capture` to regenerate the visual-audit screenshot set.
- **Status:** ✅ A, B, A-fix, C-1 all accepted into the branch. Pre-existing gate debt logged for separate cleanup.

---

## 2026-06-03 — Code review: Slice D (AI CEFR level) + Slice E (submit/sync errors)

- **Verdict:** ✅ **ACCEPTED** (both). Build + test:core green. No interiors beyond the named files; no new assets.

### Slice D — PASS
- Speaking `NachsprechenPlayer.tsx:251`: `level` now `cefrLevel ?? questEpisode?.cefrLevel ?? 'A1'`. ✅
- Grammar `ExerciseRenderer.tsx`: `cefrLevel?` prop threaded to GapFillType + Transformation; both `/api/v1/grade` payloads use `cefrLevel || 'A1'` (was `'A1'`). ✅
- Grammar `LessonPlayer.tsx`: passes `cefrLevel={level}` (real lesson level) into `ExerciseRenderer`. ✅
- Net: B1–C2 lessons now grade with the correct model/rubric.

### Slice E — PASS
- Reading `reading-player.tsx` + Listening `lesson-player.tsx`: submit throws on `!res.ok`/`!success`; bilingual learner-facing error banner; phase stays on exercise → **answers preserved**; retry via the existing submit CTA; reset clears error. ✅
- Grammar `LessonPlayer.tsx`: progress save refactored to async try/catch with `syncError` state + amber "Thử lại" retry banner; effect won't auto-retry after failure (no loop). ✅
- SRS `review-client.tsx`: both sync paths (Web Worker + fallback) detect failure and push to a deduped `failedSyncs` list; `retrySync` re-POSTs and clears succeeded; non-blocking amber banner (lists affected words) + "Thử lại" in all 3 view modes; learner is not trapped and not falsely told it saved. ✅
- Nits (non-blocking): SRS banner JSX duplicated 3× (extractable); reading/listening "retry" is the existing submit button (acceptable).

### Gates (independently run by QC)
- `pnpm build`: ✅ PASS (37.8s). `pnpm test:core`: ✅ PASS (web suite + ai-service 58/58).
- Pre-existing gate debt unchanged (asset-audit / locale-parity / visual-audit) — see CW-1/2/3. (Slice E added bilingual error strings inline; these add to the locale-parity backlog tracked in CW-2.)
- **Status:** ✅ D + E accepted into the branch. Remaining open: CW-1/2/3 cleanup work orders; optional Slice C quest-visuals restyle.

---

## 2026-06-03 — Code review: cleanup work orders CW-1 + CW-2 (Batch 1) + CW-3

Note: Antigravity's pasted report covered only CW-2 Batch 1, but the working tree contained CW-1 and CW-3 as well — QC'd all three. Gate-integrity checked (not gamed).

- **CW-1 (asset-audit) ✅:** `computeCoverage` recalibrated to `referenced / (total − archived)` (counts archived separately, doesn't inflate numerator; orphans/forbidden/threshold checks intact). Legitimate. `check:asset-audit` ✅ **100% (50/50), 0 orphans**.
- **CW-2 Batch 1 (i18n) ✅:** de.json/vi.json parity (232 keys); `MultipleChoice.tsx` / `DashboardMockupClient.tsx` / `SessionPlayer.tsx` → **0 locale-parity violations**; string→`t()` only, Slice A/B logic untouched; `pnpm build` green. Remaining locale violations (`writing-player.tsx` etc.) are future batches.
- **CW-3 (visual-audit) ✅:** 44 **real** PNG captures (6.4 MB) generated; `run-marker-flip.ts` flips manifest markers only when the PNG exists (`existsSync`-gated, not faked); `_helpers.ts` adds `result` to the valid-state regex. `check:visual-audit` ✅ (44 verified, 4 invariants pass). Minor: captures filed under the stale `2026-05-16` folder date.

### Gate status now
- ✅ `check:asset-audit`, ✅ `check:visual-audit`, ✅ `pnpm build`, ✅ `pnpm test:core` (prior).
- `check:quick` now advances past asset-audit and stops at `check:locale-parity` (CW-2 Batch 2+ pending) — improved, not yet fully green.

### Note (committed evidence)
CW-3 commits ~6.4 MB of capture PNGs under `docs/design/visual-audit/qa-runs/2026-05-16/screenshots/` as QA evidence (the gate verifies they exist on disk). If the repo prefers CI-generated captures, gitignore the folder and run `pnpm test:integration:capture` in CI instead.
- **Status:** ✅ CW-1/2/3 accepted. Folded onto the PR #20 branch (CW-2 depends on the Slice A/B file versions).
